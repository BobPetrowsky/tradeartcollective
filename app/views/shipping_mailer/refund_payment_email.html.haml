!!!
%html
  %head
    %meta(charset="utf-8")
  %body
    %h1 Your payment has been refunded!
    %div
      %p= image_tag @sale.item.images[0].image
      %p= @sale.item.name
      %p= "$#{@sale.item.price}"
      %p= @sale.item.description
    %h2 Shipped to:
    %div
      %p=@sale.shipping.name
      %p=@sale.shipping.address1
      %p=@sale.shipping.address2
      %p=@sale.shipping.city
      %p=@sale.shipping.state
      %p=@sale.shipping.zip
      %p=@sale.shipping.country
